name: Smoke Test

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Image version to test (e.g. 1.1.7)'
        required: true

permissions:
  contents: read
  id-token: write

jobs:
  smoke-test:
    runs-on: ubuntu-latest

    steps:
      - name: Setup JFrog Fly
        uses: jfrog/fly-action@v1
        with:
          url: https://cga.jfrog.io

      - name: Pull Docker image
        run: docker pull cga.jfrog.io/docker/${{ github.repository }}:${{ github.event.inputs.version }}

      - name: Start container
        run: |
          docker run -d --name smoke-test-container -p 8080:80 cga.jfrog.io/docker/${{ github.repository }}:${{ github.event.inputs.version }}
          sleep 3

      - name: Run smoke test
        run: |
          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080)
          echo "HTTP status: $HTTP_STATUS"
          if [ "$HTTP_STATUS" != "200" ]; then
            echo "Smoke test failed — expected 200, got $HTTP_STATUS"
            exit 1
          fi
          echo "Smoke test passed"

      - name: Check security headers
        run: |
          HEADERS=$(curl -s -I http://localhost:8080)
          echo "$HEADERS"
          for HEADER in "X-Content-Type-Options" "X-Frame-Options" "X-XSS-Protection" "Referrer-Policy"; do
            if echo "$HEADERS" | grep -qi "$HEADER"; then
              echo "✓ $HEADER present"
            else
              echo "✗ $HEADER missing"
              exit 1
            fi
          done

      - name: Stop container
        if: always()
        run: docker rm -f smoke-test-container
